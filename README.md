# ft_printf
A custom C library that replicates the standard printf() function. It supports variadic arguments and core conversions like %c, %s, %p, %d, %i, %u, %x, %X, and %% . Built for 42 school with a focus on extensible, well-structured code.
